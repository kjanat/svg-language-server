# Progress Log

PRD: full-snapshot-svg-data
Started: 2026-04-09

## Codebase Patterns

- Canonical truth must be stored per snapshot, not as union-plus-exceptions.
- Union data and version overlays are derived artifacts only.
- SVG 1.1 snapshots rely on TR indices plus flattened DTDs for structure.
- SVG 2 CR uses W3C TR as authority even when GitHub assists extraction.
- SVG 2 ED should parse the pinned `w3c/svgwg` commit and `definitions*.xml` files directly.
- Raw fetched source bundles stay out of git; pinned manifests and normalized data stay in git.
- Value grammar is first-class data, not only display text.
- External specs start as pinned typed foreign references, not full ingest.
- Source acquisition is staged as checked-in TOML manifests under `crates/svg-data/data/sources/`, one file per snapshot or external spec set.
- Snapshot schemas should use serde-tagged enums plus canonical `SpecSnapshotId` values so checked-in JSON stays explicit and stable.
- Shared snapshot ingestion should flow through four reusable steps: typed manifest load, cache-backed fetch, manifest-derived provenance, and stable per-snapshot JSON emission.
- Seed new checked-in snapshot datasets from the current profile-aware catalog first, then freeze parity with a dataset-specific regression test before replacing the seed with source-native parsing.
- Shared seed generators should derive review text and pinned-source coverage from the selected snapshot/manifest so one path can safely emit multiple snapshot variants.
- Shared snapshot seeds should bind provenance inputs per snapshot so one generator can swap DTD-backed SVG 1.1 facts for TR-backed SVG 2 facts without forking the write path.
- Snapshot seed provenance cannot assume shared `element-index` or `attribute-index` inputs; bind those ids and locator styles per snapshot manifest.
- Seed grammar normalization can stay deterministic by deriving shared grammar ids from `AttributeValues` variants and per-attribute enum grammar ids from attribute names, while leaving only `FreeText` as the explicit opaque escape hatch.
- SVG 2 foreign refs should add only the actually used external pins to `snapshot.json` and append manual-review provenance instead of replacing the snapshot-local source trail.
- `review.json` should be derived from checked-in snapshot facts plus preserved human notes, so audit fields stay reproducible and manual context stays append-only.

---

## Task - [source-manifests]

- Added pinned source manifests for all four tracked snapshots plus a foreign-reference manifest under `crates/svg-data/data/sources/`.
- Added `crates/svg-data/tests/source_manifests.rs` to verify manifest coverage for snapshot ids and required manifest fields.
- Updated `crates/svg-data/build.rs` to watch `data/sources/` for future pipeline work.
- **Learnings:** `dprint` aligns TOML keys, so manifest validation should check keys semantically instead of exact spacing; the 2025-09-14 editor's draft pin maps cleanly to `w3c/svgwg` commit `19482daf4094e72becde92b38c6a1c0d384b56a9`.

## Task - [snapshot-schema]

- Added `crates/svg-data/src/snapshot_schema.rs` with typed serde-backed schemas for `snapshot.json`, per-snapshot fact files, provenance, exceptions, reviews, and the normalized grammar AST.
- Reused canonical `SpecSnapshotId` values in checked-in snapshot metadata by deriving serde support in `crates/svg-data/src/types.rs`, and exported the new module from `crates/svg-data/src/lib.rs`.
- **Learnings:** keep schema shapes tagged and typed up front; it prevents ambiguous JSON now and gives the extractor pipeline a stable contract later.

## Task - [extractor-framework]

- Added `crates/svg-data/src/extraction.rs` with typed source-manifest loading, cache-backed fetch helpers, manifest-derived snapshot metadata/provenance builders, and stable per-snapshot JSON dataset writing.
- Updated `crates/svg-data/src/lib.rs` and `crates/svg-data/Cargo.toml` to expose the extractor helpers and support TOML parsing, real SHA-256 cache sidecars, and tempdir-backed tests.
- Ran `cargo fmt --all`, `cargo clippy --workspace --all-targets --all-features -- -D clippy::all`, `cargo test -p svg-data`, `cargo test -p svg-lint`, and `cargo test -p svg-language-server`.
- **Learnings:** keeping fetch logic generic over caller-provided network loaders lets the shared framework own cache/provenance/write behavior without forcing live HTTP into the runtime crate API.

## Task - [svg11-2003-ingest]

- Added `crates/svg-data/examples/generate_snapshot_seed.rs` to deterministically seed SVG 1.1 snapshot datasets from the current profile-aware catalog and manifest-backed provenance.
- Generated and checked in `crates/svg-data/data/specs/Svg11Rec20030114/` with snapshot metadata, elements, attributes, categories, applicability edges, empty grammar/exception placeholders, and a review report.
- Added `crates/svg-data/tests/svg11_first_snapshot.rs` to lock the checked-in first-edition dataset to current profile-aware element, attribute, and applicability behavior.
- Ran `cargo fmt --all`, `cargo check --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D clippy::all`, `cargo test -p svg-data`, `cargo test -p svg-lint`, and `cargo test -p svg-language-server`.
- **Learnings:** seeding from the existing profile-aware catalog is a fast way to establish checked-in snapshot truth, but keeping the generator explicit and the parity test strict makes it safe to replace with source-native extraction later.

## Task - [svg11-2011-ingest]

- Generated and checked in `crates/svg-data/data/specs/Svg11Rec20110816/` with snapshot metadata, elements, attributes, categories, applicability edges, empty grammar/exception placeholders, and a review report.
- Fixed `crates/svg-data/examples/generate_snapshot_seed.rs` so review notes derive from the selected snapshot instead of hard-coding the 2003 id, then extended `crates/svg-data/tests/svg11_first_snapshot.rs` to lock both SVG 1.1 seeds to current profile-aware behavior.
- Ran `cargo fmt --all`, `cargo check --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D clippy::all`, and `cargo test --workspace`.
- **Learnings:** the 2011 seed uses the same profile-backed catalog shape as 2003, but its manifest contributes an extra pinned source for the TR-linked HTML archive cross-check, so parity tests should assert manifest-derived source counts per snapshot.

## Task - [svg2-2018-ingest]

- Extended `crates/svg-data/examples/generate_snapshot_seed.rs` to support `Svg2Cr20181004` by binding snapshot-specific provenance inputs and review notes while keeping the shared seed path intact.
- Generated and checked in `crates/svg-data/data/specs/Svg2Cr20181004/` with snapshot metadata, elements, attributes, categories, applicability edges, empty grammar/exception placeholders, and a CR-specific review report.
- Extended `crates/svg-data/tests/svg11_first_snapshot.rs` with SVG 2 CR regression coverage for profile parity, pinned-source count, and the `href` versus `xlink:href` split.
- Ran `cargo fmt --all`, `cargo check --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D clippy::all`, and `cargo test --workspace`.
- **Learnings:** the current catalog is good enough to seed SVG 2 CR snapshot truth, but the seed must record HTML-backed provenance and explicit review notes so later source-native extraction can replace it without blurring SVG-owned facts versus foreign follow-up work.

## Task - [svg2-2025-ingest]

- Extended `crates/svg-data/examples/generate_snapshot_seed.rs` to seed `Svg2EditorsDraft20250914` from the pinned draft manifest, including snapshot-specific provenance bindings for definitions-backed facts.
- Generated and checked in `crates/svg-data/data/specs/Svg2EditorsDraft20250914/` with snapshot metadata, elements, attributes, categories, applicability edges, empty grammar/exception placeholders, and a draft-specific review report.
- Extended `crates/svg-data/tests/svg11_first_snapshot.rs` with regression coverage for the editor's draft seed so pinned source count, profile parity, and draft review notes stay locked.
- Ran `cargo fmt --all`, `cargo check --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D clippy::all`, and `cargo test --workspace`.
- **Learnings:** the draft manifest does not expose TR-style index inputs, so the shared seed path needs per-snapshot provenance bindings for element and attribute coverage instead of assuming every source bundle looks like a REC or TR snapshot.

## Task - [grammar-normalization]

- Extended `crates/svg-data/examples/generate_snapshot_seed.rs` to emit deterministic `grammars.json` payloads and attach `grammar_ref` value syntax for shared SVG datatypes, transform lists, path data, points, viewBox, preserveAspectRatio, URL references, and per-attribute enums.
- Regenerated all four checked-in snapshot datasets under `crates/svg-data/data/specs/` so review counts and attribute syntax now reflect normalized grammar coverage instead of opaque placeholders.
- Extended `crates/svg-data/tests/svg11_first_snapshot.rs` to assert grammar counts, shared grammar presence, and that every non-`FreeText` catalog attribute present in a snapshot resolves to a checked-in grammar id.
- **Learnings:** driving grammar seeding from `AttributeValues` gives broad structured coverage with minimal new extraction code; the remaining opaque cases are the intentional free-form text boundary, not a silent fallback for SVG-owned syntax.

## Task - [foreign-typed-refs]

- Extended `crates/svg-data/examples/generate_snapshot_seed.rs` so SVG 2 seeds attach only the used foreign spec pins and remap CSS-backed, masking, filter/compositing, and animation value syntax to typed `foreign_ref` entries with appended manual-review provenance.
- Regenerated `crates/svg-data/data/specs/Svg2Cr20181004/` and `crates/svg-data/data/specs/Svg2EditorsDraft20250914/` so their snapshot metadata, attribute syntax, and review notes reflect the pinned foreign references.
- Extended `crates/svg-data/tests/svg11_first_snapshot.rs` to assert the exact SVG 2 foreign pins, foreign value targets, and retained non-SVG2 behavior.
- **Learnings:** record-level attribute provenance is enough for seeded foreign refs as long as the snapshot keeps its native provenance entries and appends one explicit foreign manual-review provenance record per remapped value syntax.

## Task - [snapshot-reviews]

- Added a deterministic `svg_data::review` audit path that derives `review.json` from checked-in snapshot facts, including applicability coverage, provenance coverage, exception inventory, and unresolved issue checks.
- Added `crates/svg-data/examples/generate_snapshot_review.rs`, `crates/svg-data/tests/snapshot_reviews.rs`, and regenerated all checked-in `review.json` files under `crates/svg-data/data/specs/`.
- Ran `cargo fmt --all`, `cargo check --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D clippy::all`, and `cargo test --workspace`.
- **Learnings:** keep review notes human-owned but derive every audit field from the normalized snapshot files; that keeps review drift testable and regeneration cheap.
