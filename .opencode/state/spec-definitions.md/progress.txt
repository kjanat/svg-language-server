# Progress Log

PRD: spec-definitions.md
Spec: spec-definitions.md
Started: 2026-04-09

## Codebase Patterns

- Keep `svg-data` public profile metadata in `src/lib.rs` + `src/types.rs` first when it is small and static; save build-script churn for later profile-specific catalog overlays.
- Normalize user-facing identifiers by stripping punctuation and folding ASCII case when config should accept canonical ids, aliases, and human-written long-form names.
- Keep spec lifecycle as its own field on `svg-data` defs; compat booleans stay a separate axis so later lint/LSP code can switch without re-plumbing catalog generation again.
- When profile deltas are sparse, keep the catalog as a union and layer snapshot membership tables in `svg-data::lib`; that gives lint/LSP profile-aware lookups without forcing generated catalog churn yet.

---

## Task - snapshot-metadata

- Added `SpecSnapshotId` and `SpecSnapshotMetadata` to `crates/svg-data/src/types.rs`
- Added `svg_data::spec_snapshots()`, `snapshot_metadata()`, and `resolve_profile_id()` in `crates/svg-data/src/lib.rs`
- Pinned metadata now covers source URL, snapshot date, stable base, errata folding, plus friendly aliases and long-form synonyms for all four target snapshots
- Added regression tests for alias resolution, case-insensitive parsing, long-form synonym parsing, and metadata invariants
- Files changed: `crates/svg-data/src/lib.rs`, `crates/svg-data/src/types.rs`, `.opencode/state/spec-definitions.md/prd.json`, `.opencode/state/spec-definitions.md/progress.txt`
- **Learnings:** this repo has no existing `.opencode/state` entry for `spec-definitions.md`, so the workflow state had to be bootstrapped from the checked-in spec before the task could be tracked

## Task - spec-lifecycle

- Added a dedicated `SpecLifecycle` enum and `spec_lifecycle` field on `ElementDef` and `AttributeDef`
- Updated `svg-data` codegen so spec lifecycle comes only from pinned spec metadata while existing `deprecated` and `experimental` flags remain browser/runtime compat signals
- Added a regression test proving `xlink:href` stays `SpecLifecycle::Stable` even though compat marks it deprecated
- Files changed: `crates/svg-data/build.rs`, `crates/svg-data/src/catalog.rs`, `crates/svg-data/src/lib.rs`, `crates/svg-data/src/types.rs`, `.opencode/state/spec-definitions.md/prd.json`, `.opencode/state/spec-definitions.md/progress.txt`
- **Learnings:** `svg-data` catalog codegen already centralizes field wiring, so separating lifecycle semantics was a one-pass change once the shared type lived in `src/types.rs`

## Task - profile-lookups

- Added `ProfileLookup`, `ProfiledElement`, and `ProfiledAttribute` in `crates/svg-data/src/types.rs`
- Added `element_for_profile`, `attribute_for_profile`, `elements_with_profile`, `allowed_children_with_profile`, and `attributes_for_with_profile` in `crates/svg-data/src/lib.rs`
- Kept the current catalog as a union and layered explicit snapshot membership tables for `href` vs `xlink:*`, so SVG 1.1 and SVG 2 profiles now resolve the right attribute surface without build-script churn
- Added regression tests for present vs unsupported vs unknown lookups and for `use` switching between `xlink:href` and `href` by profile
- Files changed: `crates/svg-data/src/lib.rs`, `crates/svg-data/src/types.rs`, `.opencode/state/spec-definitions.md/prd.json`, `.opencode/state/spec-definitions.md/progress.txt`
- **Learnings:** filtering profile-aware attribute lists at lookup time is enough for the current dataset because child policies and most union entries are unchanged across snapshots; only the sparse membership deltas need explicit overlays
