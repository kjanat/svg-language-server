[toolchain]
channel    = "nightly"
profile    = "default"
components = ["rustfmt", "clippy", "llvm-tools", "cargo", "rust-analyzer"]
# targets    = ["wasm32-unknown-unknown", "wasm32-wasip2", "x86_64-unknown-linux-musl"]
